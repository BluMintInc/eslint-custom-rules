# Enforce that functions with assert- prefix must throw an error or call process.exit(1), and functions that call assert- prefixed methods should be prefixed with assert- themselves (`@blumintinc/blumint/enforce-assert-throws`)

💼 This rule is enabled in the ✅ `recommended` config.

<!-- end auto-generated rule header -->
